const menuBtn=document.getElementById('menuBtn');const navMenu=document.getElementById('navMenu');if(menuBtn){menuBtn.addEventListener('click',()=>navMenu.classList.toggle('open'));}
document.querySelectorAll('.ba-slider').forEach(slider=>{const after=slider.querySelector('.ba-after');const handle=slider.querySelector('.ba-handle');let active=false;const set=x=>{const r=slider.getBoundingClientRect();let p=((x-r.left)/r.width)*100;p=Math.max(3,Math.min(97,p));after.style.width=p+'%';handle.style.left=p+'%';const img=after.querySelector('img');if(img)img.style.width=(10000/p)+'%'};['mousedown','touchstart'].forEach(ev=>slider.addEventListener(ev,e=>{active=true;set(e.touches?e.touches[0].clientX:e.clientX)}));['mousemove','touchmove'].forEach(ev=>window.addEventListener(ev,e=>{if(active)set(e.touches?e.touches[0].clientX:e.clientX)}));['mouseup','touchend'].forEach(ev=>window.addEventListener(ev,()=>active=false));});
